name: 'weather_markets'
version: '1.0.0'
config-version: 2

profile: 'weather_markets'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  weather_markets:
    staging:
      +materialized: view
    marts:
      +materialized: table
